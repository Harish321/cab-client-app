# React App Configuration
PORT=80

# This file sets the port for the React development server
# Port 80 is the default HTTP port and may require administrator privileges
# Run with: sudo npm start (on macOS/Linux) if permission issues occur
